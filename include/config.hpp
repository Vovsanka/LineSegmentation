#ifndef CONFIG_HPP
#define CONFIG_HPP

const int DIRECTIONS = 360;
const double R = 1.5; // radius for the circle of interesting pixels
const double STEP = 0.1; // step size for the discrete subpixels in the circle
const double OFFSET = 40;
const double THICKNESS = 0.2; // line thickness
const double CAND_RATIO = 5.0; // candidate ratio
const double LOGISTIC_MID = 0.5;

#endif